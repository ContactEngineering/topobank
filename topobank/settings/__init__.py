"""Settings for TopoBank.

Every ``TOPOBANK_*`` setting has a default in `defaults`. Code reads a setting as
``getattr(settings, "TOPOBANK_X", defaults.TOPOBANK_X)``, so a deployment only
configures what it wants to change.
"""
